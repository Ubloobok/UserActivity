<UserControl
	x:Class="UserActivity.Viewer.View.ActivitiesDataGridView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	mc:Ignorable="d" 
	d:DesignHeight="300" d:DesignWidth="300">
	<Grid>
		<DataGrid
			IsReadOnly="True" AutoGenerateColumns="False"
			ItemsSource="{Binding Path=Activities, Mode=OneWay}">
			<DataGrid.Columns>
				<DataGridTextColumn Header="Дата/Время (UTC)" Binding="{Binding Path=UtcDateTimeString, Mode=OneWay}" />
				<DataGridTextColumn Header="Вид" Binding="{Binding Path=Kind, Mode=OneWay}" />
				<DataGridTextColumn Header="X" Binding="{Binding Path=InRegionX, Mode=OneWay}" />
				<DataGridTextColumn Header="Y" Binding="{Binding Path=InRegionY, Mode=OneWay}" />
				<DataGridTextColumn Header="Регион" Binding="{Binding Path=RegionName, Mode=OneWay}" />
				<DataGridTextColumn Header="Изображение" Binding="{Binding Path=ImageName, Mode=OneWay}" />
			</DataGrid.Columns>
		</DataGrid>
	</Grid>
</UserControl>
