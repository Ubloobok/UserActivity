<UserControl x:Class="UserActivity.Viewer.View.NewTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UserActivity.Viewer.View"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid VerticalAlignment="Center">
        <StackPanel>
            <Button
                Content="Список Событий"
                Command="{Binding Path=OpenEventListTabCommand, Mode=OneWay}"
                Style="{StaticResource LinkButton}">
            </Button>
            <Button
                Content="Тепловая Карта"
                Command="{Binding Path=OpenHeatMapTabCommand, Mode=OneWay}"
                Style="{StaticResource LinkButton}">
            </Button>
            <Button
                Content="Последовательные Шаблоны"
                Command="{Binding Path=OpenSequentialPatternTabCommand, Mode=OneWay}"
                Style="{StaticResource LinkButton}">
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
